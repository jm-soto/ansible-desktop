---
# Docker packages.
# After set the docker repository, run this command to 
# list available docker-ce versions:
#
#   $ apt-cache madison docker-ce
#
docker_package:
  - "docker-ce=5:26.1.3-1~ubuntu.20.04~focal"
  - "docker-ce-cli"
  - containerd.io
docker_package_state: present

